//
//  File.swift
//  SubScriptNotebook
//
//  Created by Colbyn Wadman on 11/8/22.
//

import SwiftUI

/// The name UX doesn’t really mean anything, it was originally UI (i.e. “user interface”), but since traits can’t be
/// namespace under UI, I ended up preexisting such traits with UI, and obviously this kinda clashes with the UI
/// prefix from UIKit, so I renamed UI to UX.
struct UX {}
